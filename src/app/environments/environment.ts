import { API_ENDPOINTS } from './api-endpoints';

export const environment = {
    production: false,
    api: API_ENDPOINTS.local.api,
    fast_edge: API_ENDPOINTS.fast_edge,
    nctv: API_ENDPOINTS.nctv,
};
