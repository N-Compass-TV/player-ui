import { PlayerDetailsDirective } from './player-details.directive';

describe('PlayerDetailsDirective', () => {
    it('should create an instance', () => {
        const directive = new PlayerDetailsDirective();
        expect(directive).toBeTruthy();
    });
});
