/** Defines locally saved zone data */
export interface LPlayerZone {
    background: string;
    height: number;
    playlist_id: string;
    playlist_type: string | null;
    screen_id: string;
    template_id: string;
    width: number;
    x_pos: number;
    y_pos: number;
    zone_order: number;
}
